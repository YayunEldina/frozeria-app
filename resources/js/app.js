import './bootstrap';
import Alpine from 'alpinejs';

window.Alpine = Alpine;

// Tambahkan baris ini untuk mengetes di console browser nanti
console.log('Alpine JS sedang dimuat...'); 

Alpine.start();